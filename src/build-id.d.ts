// Injected by vite.config.ts via `define`: a short git commit hash (e.g. "a1b2c3d"),
// or the string 'dev' if the hash could not be determined (e.g. git unavailable).
declare const __BUILD_ID__: string;
